import {GraphQLClient, gql} from 'graphql-request';

const authKey = '';
const endpoint = '';

class WebinyBenchmark {
  constructor(sampleDataFilename) {
    this.sampleDataFilename = sampleDataFilename;
  }

  getRegion(regionName) {}

  getCountry(countryName) {}

  getItemType(itemTypeName) {}

  getSalesChannel(salesChannelName) {}

  getOrderPriority(orderPriority) {}

  getOrder(order) {}
}
